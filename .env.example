# Backend
HOST=0.0.0.0
PORT=8080

# Agent client connection (used by client.py if CONTROLLER_URL is set)
CONTROLLER_URL=http://localhost:8080

# Frontend build-time variable (if you adapt UI to use Vite env)
VITE_SOCKET_URL=http://localhost:8080

