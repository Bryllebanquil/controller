# ============================================================================
# Complete Requirements for client.py - All Dependencies
# ============================================================================
# Python Version: 3.8+ (Fully compatible with Python 3.14)
# Installation: pip install -r requirements-client.txt
# Or with pre-release versions: pip install --pre -r requirements-client.txt
# Updated: 2025-10-15 for Python 3.14 compatibility
# ============================================================================

# Core networking & Socket.IO (CRITICAL for controller connection)
--pre python-socketio>=5.12.0
--pre python-engineio>=4.11.0
--pre requests>=2.32.0
--pre urllib3>=2.3.0
--pre certifi>=2024.12.0
--pre websocket-client>=1.8.0
--pre websockets>=14.1

# Async & Event handling
--pre eventlet>=0.37.0

# DNS resolution
--pre dnspython>=2.0.0

# Windows-specific libraries (pywin32)
--pre pywin32>=306
--pre comtypes>=1.2.0

# Computer Vision & Image Processing
--pre opencv-python>=4.10.0
--pre numpy>=2.0.0
--pre Pillow>=11.0.0
--pre mss>=9.0.0

# GUI Automation & Input Control
--pre pyautogui>=0.9.54
--pre pynput>=1.7.6
--pre pygame>=2.5.0

# Audio Processing
--pre pyaudio>=0.2.13
--pre SpeechRecognition>=3.10.0
--pre gTTS>=2.5.0

# System Monitoring
--pre psutil>=5.9.0

# WebRTC & Video Streaming
--pre aiortc>=1.6.0
--pre av>=11.0.0

# Cryptography & Security
--pre cryptography>=44.0.0

# Web Framework (for controller mode)
--pre Flask>=3.0.0
--pre Flask-SocketIO>=5.3.0

# Machine Learning & AI (optional but used in code)
--pre tensorflow>=2.18.0
--pre torch>=2.5.0
--pre torchvision>=0.20.0

# Additional utilities
--pre lxml>=5.3.0
--pre beautifulsoup4>=4.12.0
--pre selenium>=4.27.0
--pre pyppeteer>=2.0.0

# HTTP/Web scraping
--pre httpx>=0.28.1
--pre aiohttp>=3.11.0

# Data handling
--pre pandas>=2.2.0
--pre openpyxl>=3.1.0

# Compression
--pre lz4>=4.3.0

# JSON/Data validation
--pre jsonschema>=4.23.0
--pre pydantic>=2.10.0

# Misc utilities
--pre python-dateutil>=2.8.0
--pre pytz>=2024.1
--pre colorama>=0.4.6
--pre tqdm>=4.66.0

# Database (if needed)
--pre pymongo>=4.10.0
--pre redis>=5.2.0

# Cloud services
--pre google-cloud-storage>=2.18.0
--pre boto3>=1.35.0

# Jupyter/IPython (for interactive features)
--pre ipython>=8.31.0
--pre jupyter>=1.1.0

# Networking tools
--pre paramiko>=3.4.0
--pre scapy>=2.5.0

# Protocol buffers & RPC
--pre protobuf>=5.29.0
--pre grpcio>=1.69.0

# Image manipulation
--pre imageio>=2.36.0
--pre scikit-image>=0.24.0

# PyInstaller (for compilation)
--pre pyinstaller>=6.12.0

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================
#
# Install all packages (recommended):
#   pip install --pre -r requirements-client.txt
#
# Install minimal required packages only:
#   pip install --pre python-socketio python-engineio requests urllib3 certifi
#   pip install --pre pywin32 opencv-python numpy Pillow psutil pynput pyautogui
#
# Install for compilation:
#   pip install --pre pyinstaller
#
# ============================================================================
