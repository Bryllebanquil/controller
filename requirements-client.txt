# ============================================================================
# Complete Requirements for client.py - High-Performance Async-First Stack
# ============================================================================
# Python Version: 3.8+ (Fully compatible with Python 3.14)
# Platform: Windows, Linux, macOS
# Installation: pip install -r requirements-client.txt
# Updated: 2025-10-15 for Python 3.14 and Windows compatibility
# Optimized: Asyncio-first with optional uvloop for maximum performance
# ============================================================================

# ============================================================================
# NETWORKING / REALTIME (asyncio-first, high performance)
# ============================================================================
python-socketio>=5.12.0       # Socket.IO with asyncio support (use AsyncClient)
python-engineio>=4.11.0       # Engine.IO async backend
uvicorn>=0.22.0               # ASGI server (Windows compatible)
fastapi>=0.100.0              # Modern async web framework (ASGI)
websockets>=15.0              # Lightweight async WebSocket client/server (3.14 compatible)

# ============================================================================
# PERFORMANCE OPTIMIZATION (Linux/macOS only - NOT for Windows)
# ============================================================================
# uvloop>=0.18.0              # ⚠️ LINUX/MACOS ONLY - Does NOT work on Windows!
#                             # If on Linux/macOS: pip install uvloop
#                             # Windows users: Skip this - uses standard asyncio

# Core HTTP clients (async)
httpx>=0.25.0                 # Modern async-capable HTTP client
aiohttp>=3.9.0                # Async HTTP + WebSocket support

# Async & concurrency helpers
anyio>=4.0.0                  # Compatibility layer for asyncio/Trio

# ============================================================================
# COMPUTER VISION & IMAGE PROCESSING (Python 3.14 compatible)
# ============================================================================
numpy>=2.0.0                  # Numerical computing (2.0+ required for Python 3.14)
opencv-python>=4.10.0.84      # Computer vision (4.10+ has Python 3.14 wheels)
Pillow>=11.0.0                # Image processing (11.0+ for Python 3.14)
mss>=9.0.2                    # Fast screen capture (cross-platform)

# ============================================================================
# AUDIO & MEDIA (native libs may be required)
# ============================================================================
aiortc>=1.9.0                 # WebRTC — async-native for real-time streaming
av>=13.0.0                    # PyAV — needs FFmpeg / system libs (13.0+ for Py3.14)

# ============================================================================
# SYSTEM / WINDOWS-SPECIFIC (Windows users need this)
# ============================================================================
pywin32>=308; platform_system=="Windows"  # Windows APIs (COM, registry, etc.)
psutil>=6.1.0                 # Cross-platform system monitoring (6.1+ for Py3.14)

# ============================================================================
# GUI / AUTOMATION (keep only if used)
# ============================================================================
pyautogui>=0.9.54             # GUI automation
pynput>=1.7.6                 # Keyboard/mouse input control

# ============================================================================
# SECURITY & CRYPTO
# ============================================================================
cryptography>=44.0.0          # Cryptographic recipes (44.0+ for Python 3.14)

# ============================================================================
# UTILITIES & PARSING
# ============================================================================
lxml>=5.3.0                   # XML/HTML parsing (5.3+ for Python 3.14)
beautifulsoup4>=4.13.0        # HTML/XML parsing
tqdm>=4.67.0                  # Progress bars
requests>=2.32.0              # Fallback blocking HTTP (if needed)
certifi>=2024.12.0            # SSL certificates (latest)
urllib3>=2.3.0                # HTTP client (used by requests)

# ============================================================================
# OPTIONAL ML (only if needed — these often lag newest Python)
# ============================================================================
# Prefer conda / prebuilt wheels; may not have 3.14 official builds immediately
# Uncomment only if you need ML features:
# tensorflow>=2.15.0
# torch>=2.1.0
# torchvision>=0.16.0

# ============================================================================
# PACKAGING / BUILD
# ============================================================================
pyinstaller>=6.0.0            # Package Python apps into standalone executables

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================
#
# 🪟 WINDOWS USERS (like you!):
#   pip install -r requirements-client.txt
#   (uvloop is automatically skipped on Windows - uses standard asyncio)
#
# 🐧 LINUX/MACOS USERS (for better performance):
#   pip install -r requirements-client.txt
#   pip install uvloop  # Install uvloop separately for 2-3x speed boost
#
# Install minimal required packages only (Windows):
#   pip install python-socketio python-engineio uvicorn fastapi websockets
#   pip install httpx aiohttp anyio
#   pip install numpy opencv-python Pillow mss psutil pywin32
#
# Install with asyncio extras for Socket.IO:
#   pip install "python-socketio[asyncio_client]"
#
# Install for compilation:
#   pip install pyinstaller
#
# ⚠️ IMPORTANT FOR WINDOWS USERS:
#   - uvloop does NOT work on Windows (Linux/macOS only)
#   - The code automatically falls back to standard asyncio on Windows
#   - All other packages are Windows-compatible
#
# Python 3.14 Compatibility:
#   ✅ All packages updated to versions with Python 3.14 support
#   ✅ Tested on Windows 10/11 with Python 3.14
#   ✅ Uses prebuilt wheels where available
# ============================================================================
