# ============================================================================
# Complete Requirements for client.py - All Dependencies
# ============================================================================
# Installation: pip install -r requirements-client.txt
# Or with pre-release versions: pip install --pre -r requirements-client.txt
# ============================================================================

# Core networking & Socket.IO (CRITICAL for controller connection)
--pre python-socketio>=5.0.0
--pre python-engineio>=4.0.0
--pre requests>=2.31.0
--pre urllib3>=2.0.0
--pre certifi>=2024.0.0
--pre websocket-client>=1.6.0
--pre websockets>=12.0

# Async & Event handling
--pre eventlet>=0.33.0

# DNS resolution
--pre dnspython>=2.0.0

# Windows-specific libraries (pywin32)
--pre pywin32>=306
--pre comtypes>=1.2.0

# Computer Vision & Image Processing
--pre opencv-python>=4.8.0
--pre numpy>=1.24.0
--pre Pillow>=10.0.0
--pre mss>=9.0.0

# GUI Automation & Input Control
--pre pyautogui>=0.9.54
--pre pynput>=1.7.6
--pre pygame>=2.5.0

# Audio Processing
--pre pyaudio>=0.2.13
--pre SpeechRecognition>=3.10.0
--pre gTTS>=2.5.0

# System Monitoring
--pre psutil>=5.9.0

# WebRTC & Video Streaming
--pre aiortc>=1.6.0
--pre av>=11.0.0

# Cryptography & Security
--pre cryptography>=41.0.0

# Web Framework (for controller mode)
--pre Flask>=3.0.0
--pre Flask-SocketIO>=5.3.0

# Machine Learning & AI (optional but used in code)
--pre tensorflow>=2.15.0
--pre torch>=2.1.0
--pre torchvision>=0.16.0

# Additional utilities
--pre lxml>=4.9.0
--pre beautifulsoup4>=4.12.0
--pre selenium>=4.15.0
--pre pyppeteer>=2.0.0

# HTTP/Web scraping
--pre httpx>=0.25.0
--pre aiohttp>=3.9.0

# Data handling
--pre pandas>=2.1.0
--pre openpyxl>=3.1.0

# Compression
--pre lz4>=4.3.0

# JSON/Data validation
--pre jsonschema>=4.20.0
--pre pydantic>=2.5.0

# Misc utilities
--pre python-dateutil>=2.8.0
--pre pytz>=2024.1
--pre colorama>=0.4.6
--pre tqdm>=4.66.0

# Database (if needed)
--pre pymongo>=4.6.0
--pre redis>=5.0.0

# Cloud services
--pre google-cloud-storage>=2.14.0
--pre boto3>=1.34.0

# Jupyter/IPython (for interactive features)
--pre ipython>=8.18.0
--pre jupyter>=1.0.0

# Networking tools
--pre paramiko>=3.4.0
--pre scapy>=2.5.0

# Protocol buffers & RPC
--pre protobuf>=4.25.0
--pre grpcio>=1.60.0

# Image manipulation
--pre imageio>=2.33.0
--pre scikit-image>=0.22.0

# PyInstaller (for compilation)
--pre pyinstaller>=6.0.0

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================
#
# Install all packages (recommended):
#   pip install --pre -r requirements-client.txt
#
# Install minimal required packages only:
#   pip install --pre python-socketio python-engineio requests urllib3 certifi
#   pip install --pre pywin32 opencv-python numpy Pillow psutil pynput pyautogui
#
# Install for compilation:
#   pip install --pre pyinstaller
#
# ============================================================================
