# ============================================================================
# Complete Requirements for client.py - High-Performance Async-First Stack
# ============================================================================
# Python Version: 3.8+ (Fully compatible with Python 3.14)
# Installation: pip install -r requirements-client.txt
# Updated: 2025-10-15 for Python 3.14 compatibility
# Optimized: Asyncio-first with uvloop for maximum performance
# ============================================================================

# ============================================================================
# NETWORKING / REALTIME (asyncio-first, high performance)
# ============================================================================
python-socketio>=5.12.0       # Socket.IO with asyncio support (use AsyncClient)
python-engineio>=4.11.0       # Engine.IO async backend
uvicorn[standard]>=0.22.0     # ASGI server with uvloop + httptools for speed
fastapi>=0.100.0              # Modern async web framework (ASGI)
websockets>=11.0              # Lightweight async WebSocket client/server
uvloop>=0.18.0                # Ultra-fast asyncio event loop (2-3x faster)

# Core HTTP clients (async)
httpx>=0.25.0                 # Modern async-capable HTTP client
aiohttp>=3.9.0                # Async HTTP + WebSocket support

# Async & concurrency helpers
anyio>=4.0.0                  # Compatibility layer for asyncio/Trio

# ============================================================================
# COMPUTER VISION & IMAGE PROCESSING (prebuilt wheels recommended)
# ============================================================================
numpy>=1.26.0                 # Numerical computing (use 1.26+ for Python 3.12+)
opencv-python>=4.9.0          # Computer vision (may require build on Python 3.14)
Pillow>=10.0.0                # Image processing library
mss>=9.0.0                    # Fast screen capture

# ============================================================================
# AUDIO & MEDIA (native libs may be required)
# ============================================================================
aiortc>=1.6.0                 # WebRTC — async-native for real-time streaming
av>=11.0.0                    # PyAV — needs FFmpeg / system libs

# ============================================================================
# SYSTEM / WINDOWS-SPECIFIC (only if running on Windows)
# ============================================================================
pywin32>=306                  # Windows-specific APIs (COM, registry, etc.)
psutil>=5.9.0                 # Cross-platform system monitoring

# ============================================================================
# GUI / AUTOMATION (keep only if used)
# ============================================================================
pyautogui>=0.9.54             # GUI automation
pynput>=1.7.6                 # Keyboard/mouse input control

# ============================================================================
# SECURITY & CRYPTO
# ============================================================================
cryptography>=41.0.0          # Cryptographic recipes and primitives

# ============================================================================
# UTILITIES & PARSING
# ============================================================================
lxml>=4.9.0                   # XML/HTML parsing (fast C library)
beautifulsoup4>=4.12.0        # HTML/XML parsing
tqdm>=4.66.0                  # Progress bars
requests>=2.31.0              # Fallback blocking HTTP (if needed)
certifi>=2024.0.0             # SSL certificates
urllib3>=2.0.0                # HTTP client (used by requests)

# ============================================================================
# OPTIONAL ML (only if needed — these often lag newest Python)
# ============================================================================
# Prefer conda / prebuilt wheels; may not have 3.14 official builds immediately
# Uncomment only if you need ML features:
# tensorflow>=2.15.0
# torch>=2.1.0
# torchvision>=0.16.0

# ============================================================================
# PACKAGING / BUILD
# ============================================================================
pyinstaller>=6.0.0            # Package Python apps into standalone executables

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================
#
# Install all packages (recommended):
#   pip install -r requirements-client.txt
#
# Install minimal required packages only (high-performance async stack):
#   pip install python-socketio uvloop uvicorn[standard] fastapi websockets
#   pip install httpx aiohttp anyio
#   pip install numpy opencv-python Pillow mss psutil pywin32
#
# Install with asyncio extras for Socket.IO:
#   pip install "python-socketio[asyncio_client]"
#
# Install for compilation:
#   pip install pyinstaller
#
# Performance note: uvicorn[standard] includes uvloop + httptools for speed
# ============================================================================
