# ============================================================================
# MINIMAL WORKING REQUIREMENTS for client.py
# Python 3.11, 3.12, or 3.13 recommended
# Python 3.14 may have compatibility issues (too new!)
# ============================================================================
# Install: pip install -r requirements-minimal-working.txt
# ============================================================================

# CRITICAL: Socket.IO for server connection
python-socketio>=5.0.0
python-engineio>=4.0.0

# CRITICAL: HTTP/Network libraries
requests>=2.28.0
urllib3>=1.26.0
certifi

# CRITICAL: WebSocket support
websocket-client>=1.0.0
websockets>=10.0

# CRITICAL: Windows libraries (for Windows-specific features)
pywin32>=305; sys_platform == 'win32'

# IMPORTANT: System monitoring & control
psutil>=5.9.0

# IMPORTANT: Screen capture & image processing
mss>=6.0.0
Pillow>=9.0.0
numpy>=1.21.0

# IMPORTANT: Input control & automation
pynput>=1.7.0
pyautogui>=0.9.50
pygame>=2.1.0

# IMPORTANT: Computer vision (optional but recommended)
opencv-python>=4.5.0

# OPTIONAL: Audio features
pyaudio; sys_platform == 'win32'
SpeechRecognition>=3.8.0

# OPTIONAL: WebRTC streaming
aiortc>=1.3.0
av>=8.0.0

# OPTIONAL: Web framework (only for controller mode)
Flask>=2.0.0
Flask-SocketIO>=5.0.0

# OPTIONAL: Async HTTP
aiohttp>=3.8.0

# ============================================================================
# QUICK INSTALL COMMANDS
# ============================================================================
#
# Option 1: Install everything (recommended):
#   pip install -r requirements-minimal-working.txt
#
# Option 2: Install ONLY critical packages (fastest):
#   pip install python-socketio requests urllib3 psutil pywin32
#
# Option 3: Install without optional packages:
#   pip install python-socketio requests urllib3 psutil pywin32 mss Pillow numpy pynput
#
# ============================================================================
