# Render Production Environment Configuration
# Neural Control Hub Frontend

# Backend API URL (will be updated after backend deployment)
VITE_API_URL=https://neural-control-hub-backend.onrender.com

# WebSocket URL (same as API URL for Render)
VITE_WS_URL=https://neural-control-hub-backend.onrender.com

# Production settings
VITE_NODE_ENV=production
VITE_DEBUG=false

# Build settings
NODE_ENV=production